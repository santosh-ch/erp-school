import { FaqHighlighterDirective } from './faq-highlighter.directive';

describe('FaqHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new FaqHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
